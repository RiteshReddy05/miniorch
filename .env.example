POSTGRES_USER=miniorch
POSTGRES_PASSWORD=miniorch_dev
POSTGRES_DB=miniorch
# Used by the Spring Boot backend for HS256 JWT signing. Must be at least 32 chars.
# Generate one with: openssl rand -base64 32
MINIORCH_JWT_SECRET=change-me-in-production-use-openssl-rand-base64-32
